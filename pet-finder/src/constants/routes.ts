/* eslint no-unused-vars: 0 */
export enum routes {
  HOME = '/',
  LOGIN = '/login',
  REGISTER = '/register',
  PROFILE = '/profile',
  CONTENT = '/content',
  LOST_FORM = '/lost-form',
  NOT_FOUND = '*',
}
